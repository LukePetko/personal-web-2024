---
import "../styles/globals.css";
import HomeLayout from "./HomeLayout.astro";
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<HomeLayout title={title}>
  <div class="container mx-auto flex flex-col gap-6 p-6 font-serif text-xl">
    <div>
      <p class="text-lg">
        <a href="/" class="text-zinc-400">← Back Home</a>
      </p>
      <h1 class="font-display text-7xl font-bold">{title}</h1>
    </div>
    <slot />
  </div>
</HomeLayout>
